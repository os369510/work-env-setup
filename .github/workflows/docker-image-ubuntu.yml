name: Ubuntu Docker Image CI

on:
  push:
    branches: [ master ]
    paths:
      - 'docker/ubuntu/focal/Dockerfile'
      - 'docker/ubuntu/jammy/Dockerfile'
  pull_request:
    branches: [ master ]
    paths:
      - 'docker/ubuntu/focal/Dockerfile'
      - 'docker/ubuntu/jammy/Dockerfile'
  workflow_dispatch:

jobs:
  push_to_registry:
    name: Push Docker images to Docker Hub
    runs-on: ubuntu-latest
    steps:
      - name: Check out the repo
        uses: actions/checkout@v2

      - name: Log in to Docker Hub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME}}
          password: ${{ secrets.DOCKERHUB_PASSWD}}

      - name: Extract focal metadata (tags, labels) for Docker
        id: meta
        uses: docker/metadata-action@v3
        with:
          images: os369510/ubuntu-focal

      - name: Extract jammy metadata (tags, labels) for Docker
        id: meta
        uses: docker/metadata-action@v3
        with:
          images: os369510/ubuntu-jammy

      - name: Build and push Ubuntu focal Docker image
        uses: docker/build-push-action@v2
        with:
          context: .
          push: true
          file: docker/ubuntu/focal/Dockerfile
          tags: os369510/ubuntu-focal:latest

      - name: Build and push Ubuntu Jammy Docker image
        uses: docker/build-push-action@v2
        with:
          context: .
          push: true
          file: docker/ubuntu/jammy/Dockerfile
          tags: os369510/ubuntu-jammy:latest
